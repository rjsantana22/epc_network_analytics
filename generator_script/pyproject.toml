[project]
name = "one-project"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "pandas>=3.0.3",
    "psycopg[binary]>=3.3.4",
]

[dependency-groups]
dev = [
    "pgcli>=4.4.0",
]
